services:
  docscanai:
    build:
      target: builder
    volumes:
      - .:/app
      - docscanai-data:/app/data
      - docscanai-models:/home/docscanai/.cache
    command: uv run uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
    environment:
      - DOCSCAN_DEBUG=true
