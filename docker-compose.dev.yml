services:
  docscanai:
    volumes:
      - .:/app
      - /app/.venv          # anonymous volume — preserves venv built into the image
      - docscanai-data:/app/data
      - docscanai-models:/home/docscanai/.cache
    command: ["--reload"]
    environment:
      - DOCSCAN_DEBUG=true
